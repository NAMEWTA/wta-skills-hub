# INIT（本快照自带）

本文件属于 dated 快照，不是技能包。把本目录放到 `/workspace/` 后，对管家说：

```
读取本目录上一层或 /workspace/grok-bot-team-steward/ 里的 grok-bot-team-steward 技能。
restore 模式读取本目录。
先只读预览 ROSTER / GAPS / DROP-LIST / connectors。
我确认「按这份建」后再创建空白 Bot。
PROFILE 逐字粘贴。routine 一律暂停。
不要 Duplicate。不要宣称官方聊天已恢复。
```

若技能包不在，把本目录 `../` 找不到 SKILL.md 时：仍按 ROSTER 与每个 `bots/<slug>/PROFILE.md` 创建，MEMORY.md 发给对应 Bot 读入。
